# 🧠 ModusFlow

**Local-first AI workflow automation**  
Build, execute, and manage async workflows — all on your machine. No cloud lock-in. No accounts. Yours to control.

---

## 🚀 Features

- ⚙️ **Visual Workflow Builder** – Drag & drop with React Flow
- 🔄 **Async DAG Executor** – Parallel execution, retries, state
- 🧠 **AI Workflow Generator** – Describe in English, get a flow
- 🖥️ **Self-Hostable** – No vendor lock-in, local-first architecture
- 📦 **Node Types** – Python, HTTP, Shell, LLMs (OpenAI, Claude, etc.)
- 🧪 **Live Logs** – Monitor node-level execution in real-time
- 🔒 **No Auth, No Tracking** – Secrets via `.env`, all data stays local
- 💻 **CLI Tool** – Run, generate, and validate workflows from the terminal

---

## 📦 Install via PyPI

```bash
pip install modusflow
